defaults: &defaults
  base_uri: "https://priora.saltedge.com"
  provider_code: "artea_sandbox"

development:
  <<: *defaults
  base_uri: "https://priora.banksalt.com"
#  base_uri: "http://localhost:3000"

test:
  <<: *defaults
#  base_uri: "https://example.com"

production:
  <<: *defaults
